import { Typography } from "@material-ui/core"

const CreateAccount = () => {
    return (
        <Typography>
            CreateAccount画面
        </Typography>
    );
}
export default CreateAccount;